export class Notify {
    state : boolean;
    container : any;

    constructor() {
        let wrapper = document.createElement("div").classList.add("modal");
        //let container =
    }

    public pop(msg : string, type : number, title : string) {

    }
     public close() {

     }
}